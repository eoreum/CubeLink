# CubeLink Desktop Handoff — 2026-07-30

## 데스크탑에서 Codex에 먼저 할 말

아래 문장을 그대로 전달한다.

> Google Drive에서 받은 CubeLink 데스크탑 인수인계 ZIP을 풀었다.
> `DESKTOP_HANDOFF_2026-07-30.md`와 `AGENTS.md`를 먼저 전부 읽고, 현재 소스와
> 실제 설치 파일 상태를 확인한 다음 노트북에서 하던 작업을 이어서 진행해.
> 펌웨어는 사용자가 명시적으로 허용하기 전에는 절대 수정하거나 업로드하지 마.

## 기준 프로젝트

- GitHub 원격 저장소: `https://github.com/eoreum/CubeLink`
- 공개 웹: `https://eoreum.github.io/CubeLink/`
- 이 인수인계 소스의 Studio 버전: `v3.6.0`
- 학생용 확정 설치 파일:
  `CubeLink_Studio_v3.6.0_Student_Final.exe`
- 설치 파일 SHA-256:
  `F71E17615F8BA276026CF4E9CEE61EF373BB0D9072A76AF5FA2EC587941183BD`

## 2026-07-28~30 노트북 작업 결과

### CubeLink Studio

- 변수 생성 창을 브라우저 기본 프롬프트 대신 앱 내부 모달로 변경했다.
- 변수 이름을 Arduino 코드에서 안전한 식별자로 변환하고 전역 변수 선언을 생성한다.
- 사용자가 새 설치판에서 변수 생성·값 지정이 다시 동작함을 실제 확인했다.
- 블록 숫자·문자 입력칸이 회색으로 잠기는 포커스 문제의 복구 처리를 유지했다.
- USB 연결이 끊겼다가 돌아올 때 활성 세션을 먼저 확인하고 불필요한 복구 이동을
  하지 않도록 수정했다.
- 안전 종료는 Studio가 사전 서보 이동 명령을 보내지 않고 펌웨어에 `K` 명령
  하나만 보낸다.
- 디지털 트윈에서 핀 10 시뮬레이션 방향을 실제 로봇 방향과 맞췄다.
- 디지털 트윈의 시리얼 갱신과 화면 렌더링을 분리해 떨리는 움직임을 완화했다.
- 첫 실행 시 이전 사용자의 미션·작업공간 상태가 남지 않도록 초기화 흐름을 유지했다.
- 9개 미션 외 자유 코딩 작업공간을 제공한다.
- 수업 안정화를 위해 Studio의 조이스틱 수동모드 버튼과 주기적 시리얼 전송 코드를
  `v3.6.0`에서 제거했다. 이는 Studio UI 기능 제거이며, 펌웨어 독립 조이스틱
  후보 기능과는 별개다.

### Windows 설치 파일

- Electron/NSIS 설치 파일을 생성했다.
- 공식 WCH CH340/CH341 드라이버를 제조사 서버에서 받아 설치기에 포함했다.
- 포함 드라이버 표기 버전: `4.0.2026.02`
- 대상 장치에 `VID 1A86 / PID 7523`이 포함된다.
- 드라이버 파일과 카탈로그의 Microsoft 호환성 서명을 확인했다.
- 설치기는 컴퓨터 관리자 권한을 요청하고 `PnPUtil`로 드라이버를 등록한다.
- CubeLink 설치 파일 자체는 상용 코드 서명 인증서로 서명되지 않았다. 학교 보안
  정책에 따라 Windows 경고나 전산 담당자 허용이 필요할 수 있다.

### 확인한 검사

- `studio/web/js/app.js` JavaScript 문법 검사 통과
- `studio/web/js/blocks.js` JavaScript 문법 검사 통과
- 변수 코드 생성 및 숫자 필드 편집 가능 검사 통과
- 안전 종료 단일 `K` 명령 및 펌웨어 무변경 검사 통과
- USB 재연결 복원 및 Studio 조이스틱 수동모드 제거 검사 통과
- 패키지 안에 v3.6.0 웹 자원과 CH340 드라이버가 들어간 것을 확인
- 패키지된 Windows 앱이 정상적으로 프로세스를 시작하는 스모크 검사 통과

## 펌웨어 주의사항

- 사용자의 명시적 결정: **펌웨어는 수정하지 않는다.**
- 학생용 v3.6.0 설치 파일에도 펌웨어를 넣거나 자동 업로드하지 않았다.
- 실제 장치에 설치된 펌웨어와 이 소스 폴더의 후보 펌웨어 버전 표기가 다를 수 있다.
- 로컬 `firmware/` 파일을 최신이라고 가정하거나 장치에 올리지 말고, 실제 보드의
  응답과 사용자 확인을 먼저 받아야 한다.

## 현재 전원·하드웨어 결정

- 사용 중인 빨간 Nano I/O Expansion Shield의 GVS `V` 줄은 Nano 5V와 공통이다.
- 서보 빨간색 `+` 선은 쉴드 V 줄에서 분리해 외부 5V 전원으로 묶는다.
- 외부전원 `+`는 쉴드 V 줄에 다시 연결하지 않는다.
- 외부전원 GND, 모든 서보 GND, 쉴드 GND는 공통으로 연결한다.
- 캐패시터 `+`는 분리된 서보 플러스 묶음, 캐패시터 `-`는 공통 GND에 연결한다.
- 권장 완충 용량은 `1000µF × 2` 또는 `2200~4700µF × 1`, 정격 10V 이상
  (가능하면 16V)이다.
- PC 사용 시: Nano USB는 PC 데이터/전원, 보조배터리는 서보 전원.
- 외부 조이스틱 독립운전 시: 2포트 5V 3A 이상 전원 하나에서 한 포트는 Nano USB,
  다른 포트는 서보 외부전원으로 사용한다.
- 2포트 스마트폰 어댑터도 두 포트 동시 총출력이 `5V 3A 이상`이면 사용할 수 있다.
- 일반 PC USB 포트는 500~900mA 수준일 수 있어 서보 전원으로 권장하지 않는다.

## 데스크탑에서 소스 재개

1. ZIP을 작업용 폴더에 푼다.
2. 이 문서와 `AGENTS.md`, `docs/DECISION_LOG.md`,
   `docs/PRODUCT_ROADMAP.md`를 먼저 읽는다.
3. `studio/electron/node_modules`, `.electron-dist`, `.builder-cache`,
   `dist`는 백업 용량을 줄이기 위해 제외되어 있다.
4. 데스크탑에 기존 CubeLink Git 저장소가 있으면 먼저 백업한 뒤 원격 상태를 확인한다.
5. GitHub가 정상 접속되면 `https://github.com/eoreum/CubeLink.git`을 새 폴더에
   clone하고, 이 ZIP의 최신 소스와 비교해 의도적인 파일만 반영한다.
6. Electron 의존성은 `studio/electron/package-lock.json` 기준으로 복원한다.
7. 새 설치 파일을 만들기 전에 자동 검사와 실제 로봇 연결 검사를 다시 수행한다.

## 다음 권장 작업

- v3.6.0을 교사용 PC 한 대에 설치해 CH340 자동 등록, COM 연결, 초기화,
  블록 입력, 변수, 실시간 실행, 안전 종료까지 최종 확인한다.
- Studio에서 제거한 수동 조이스틱 기능을 다시 넣지 말고, 외부 행사형 독립
  조이스틱은 펌웨어/전원 하드웨어를 별도 검증한 뒤 다룬다.
- 물리 테스트가 끝난 뒤에만 GitHub 정식 배포와 공개 다운로드 링크를 갱신한다.
