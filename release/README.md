# CubeLink Releases

Current public release: `v3.4.2`.

CubeLink Studio `v3.4.3` and firmware `v1.4.0` are development candidates. Do
not publish a new latest release until firmware compilation and controlled
physical hardware tests pass. Publishing a latest release changes the download
served by the web intro page.

Expected Windows release asset name:

```text
Cubelink_Studio.exe
```
